---
- name: Restart a policy-authorized service
  hosts: all
  gather_facts: false
  become: true
  tasks:
    - name: Restart requested service
      ansible.builtin.service:
        name: "{{ service_name }}"
        state: restarted
