# MCP Capability Boundary

MCP standardizes interoperability; Ports and Adapters preserve domain semantics. A fixed broker
maps protocol tools to existing ports without dynamic dispatch. Models cannot choose arbitrary
servers or tools because URLs and mappings are operator configuration. MCP Tasks/MRTR do not
replace LangGraph checkpoints or durable approval.
