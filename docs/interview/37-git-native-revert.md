# Git-native Revert

An imperative rollback would reintroduce drift and bypass review. A revert is therefore a new typed
proposal, policy result, OpsPilot approval, PR, Git review, merge, pull reconciliation, and verification.
Verification failure can suggest this proposal but cannot authorize or execute it automatically.
