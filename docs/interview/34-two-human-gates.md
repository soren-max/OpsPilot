# Two Human Gates

Gate A is OpsPilot HITL: may this exact semantic ChangeSet become a PR? Gate B is Git review: may the
reviewed commit enter the protected branch? Approval A never implies B. The UI shows both, and the Git
port lacks self-review and merge capabilities.
