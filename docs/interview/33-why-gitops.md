# Why GitOps

GitOps makes desired state declarative, versioned, reviewable, pulled, and continuously reconciled.
The agent cannot bypass branch protection or mutate a cluster directly. The trade-off is asynchronous
latency and more reconciliation states, which OpsPilot models instead of hiding.
